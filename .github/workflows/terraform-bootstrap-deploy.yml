name: Terraform Bootstrap + Deploy

on:
  workflow_call:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: string
        default: 'dev'

permissions:
  id-token: write
  contents: read

env:
  ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
  ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}
  ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
  ARM_USE_OIDC: true
  environment: ${{ inputs.environment }}

jobs:
  # 🧱 1) BOOTSTRAP (runs once or idempotent)
  deploy-bootstrap:
    runs-on: ubuntu-latest
    # 👇 THIS IS THE MAGIC
    environment: ${{ inputs.environment }} 
    steps:
      - name : TF_TO_REPO_TOKEN 
        run: echo "TF_REPO_TOKEN is ${{ secrets.TF_REPO_TOKEN }}"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.TF_REPO_TOKEN }}


      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.ARM_CLIENT_ID }}
          tenant-id: ${{ secrets.ARM_TENANT_ID }}
          subscription-id: ${{ secrets.ARM_SUBSCRIPTION_ID }}

      - name: Remove bootstrap resource group if exists
        id: check_rg_bootstrap-dev
        run: |
          RG_BOOTSTRAP_DEV_EXISTS=$(az group exists -n rg-bootstrap-dev)
          echo "RG_BOOTSTRAP_DEV_EXISTS is $RG_BOOTSTRAP_DEV_EXISTS"
          if [ "$RG_BOOTSTRAP_DEV_EXISTS" = "true" ]; then
            echo "RG_BOOTSTRAP_DEV_EXISTS=true" >> $GITHUB_OUTPUT
            az group show -n rg-bootstrap-dev
          else
            echo "RG_BOOTSTRAP_DEV_EXISTS=false" >> $GITHUB_OUTPUT
            echo " rg-bootstrap-dev doesn't exist"
          fi

      - name: Terraform Init (bootstrap)
        if: steps.check_rg_bootstrap-dev.outputs.RG_BOOTSTRAP_DEV_EXISTS == 'false'
        working-directory: bootstrap
        run: terraform init

      - name: Terraform Apply (bootstrap)
        if: steps.check_rg_bootstrap-dev.outputs.RG_BOOTSTRAP_DEV_EXISTS == 'false'
        working-directory: bootstrap
        run: terraform apply -auto-approve -var="resource_group_name=rg-bootstrap-${{ inputs.environment }}" -var="storage_account_name=storageacttfstate${{ inputs.environment }}" -var="storage_container_name=tf-state-${{ inputs.environment }}"  
        
 